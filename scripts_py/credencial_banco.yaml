credenciais_banco:
  host: 'localhost' #substitua caso seja diferente
  user: 'root'   #substitua caso seja diferente
  password: 'senha do seu banco'
  database : 'candidatos_goias' #banco de dados não alterar, pois apos a conexao este será criado automaticamente  

  